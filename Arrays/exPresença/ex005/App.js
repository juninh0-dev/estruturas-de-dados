const MeuArray = require("./MeuArray");

const meuArray = new MeuArray();

/* A # é utilizada para marcar que um item é PRIVADO, ou seja, não pode ser acessado fora do escopo(da classe) normalmente.
O atributo privado impede a alteração da classe sem ser através de si mesma, ou seja, 
fazendo uma analogia para a realidade, algo privado seu pertence somente a você, 
e ninguém pode ter acesso sem ser diretamente através de ti. */
